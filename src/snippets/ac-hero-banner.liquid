<div class="c-hero-banner{{ modClass }}">

    <h1 class="c-hero-banner__heading{{ modClass }}" >{{article.title }}</h1>

    <div class="c-hero-banner__image{{ modClass }}">
      <img class="c-hero-banner__img{{ modClass }}"  src="{{ article.metafields.global.hero_image | file_url }}" alt="{{ article.title }}" >
    </div>

</div>
